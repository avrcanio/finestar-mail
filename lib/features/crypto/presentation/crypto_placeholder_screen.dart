import 'package:flutter/widgets.dart';

class CryptoPlaceholderScreen extends StatelessWidget {
  const CryptoPlaceholderScreen({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
